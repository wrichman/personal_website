<!--  <div class="row">
      
        <div class="col-lg-12">
          <h1 class="page-header">Courses</h1>
        </div>

        <% @courses.each do |course| %>
          <div class="col-lg-3 col-md-4 col-xs-6 thumb">
            <a class="thumbnail" href="<%= course_path(course) %>"><img class="img-responsive" style="height:187px;width:200px" src="<%= course.image %>">
              <h3><%= course.title %></h3> <%= link_to "Edit Course", edit_course_path(course) %>

          <div class="row">
            <div class="col-md-12">
            <small><%= truncate(course.description, length: 150) %></small>
              <div class="row">
                <div class="col-md-6">
                <%= course.subscribers.count*19 %> subscribers
                </div>
                <div class="col-md-6">
                <%= course.creator.first_name %> <%= course.creator.last_name %>
                </div>
              </div>
            </div>
          </div>
            </a>
          </div>


        <% end %>


</div> -->