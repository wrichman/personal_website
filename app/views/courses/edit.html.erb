<div class="row">
  <div class="col col-lg-5 col-offset-3"><h2>Edit a Course</h2>
    <%= form_for [@user, @course] do |f| %>
      <%= render 'shared/form_errors', :f => f %>
      <%= render 'form', :f => f %>
    <% end %>
  </div>
</div>